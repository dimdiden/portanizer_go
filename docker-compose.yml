version: '3'
services:
  portaserver:
    build: .
    env_file:
      - portaserver.env
    ports:
      - "8080:8080"
    depends_on:
      - "db"
  db:
    image: mysql:5.7
    ports:
      - "3306:3306"
    env_file:
      - db.env
    volumes:
      - /usr/local/var/mysql:/var/lib/mysql
